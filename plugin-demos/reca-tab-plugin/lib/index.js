/**
 * Host half of the ReCA tab demo.
 *
 * It intentionally has no runtime behavior. Its presence in the web profile is
 * enough for Harness to discover the package's `dsh.client` declaration.
 */
export function apply() {}
