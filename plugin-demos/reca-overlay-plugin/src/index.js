/**
 * Host half of the demo package.
 *
 * The only Host responsibility is to give the Harness loader a real package
 * entry. All product behavior lives in the browser half exported at ./client.
 */
export function apply() {}
