# Copy this file to .env. The real .env is ignored by git.

# DeepSeek planner / segment planner (OpenAI-compatible endpoint)
RECA_PLANNER_API_KEY=
RECA_PLANNER_BASE_URL=https://api.deepseek.com/v1
RECA_PLANNER_MODEL=deepseek-chat
RECA_SP_MODEL=deepseek-chat
RECA_SP_API_KEY=
RECA_SP_BASE_URL=
RECA_SP_API_PATH=chat
RECA_SP_THINKING_BUDGET=8000

RECA_PLANNER_API_PATH=chat

# ReCA's existing image/video provider variables.
# Keep these empty until the provider credentials are supplied.
DASHSCOPE_API_KEY=
DASHSCOPE_API_KEYS=
OPENAI_API_KEY=
# Optional comma-separated pool; either this or OPENAI_API_KEY configures the
# OpenAI-compatible provider used by gpt-image-2.
OPENAI_API_KEYS=
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
ZIPLAB_API_KEY=

# Vision auditor routing used by the existing ReCA validator.
RECA_SEGMENT_VALIDATOR_MODEL=
RECA_SEGMENT_VALIDATOR_FPS=8
RECA_SEGMENT_VALIDATOR_ON_ERROR=raise

# GPT internal Responses API for the visual validator.
RECA_GPT_API_KEY=
RECA_GPT_RESPONSES_URL=
RECA_GPT_MODEL=gpt-5.6-sol
RECA_GPT_SERVICE_TIER=fast
# The Gateway entry point maps RECA_GPT_API_KEY to OPENAI_API_KEY when no
# OPENAI_API_KEY is set, so it can also satisfy gpt-image-2 credentials.
RECA_DISABLE_HTTP_PROXY=1
# Cross-process limiter and transient upstream retry policy for GPT Vision.
RECA_GPT_GLOBAL_LOCK_PATH=/tmp/reca-gpt-responses.lock
RECA_GPT_MAX_RETRIES=3
RECA_GPT_RETRY_BASE_S=2

# Wan 3.0 (Alibaba Model Studio / DashScope async video API).
RECA_WAN30_API_KEY=
RECA_WAN30_BASE_URL=https://dashscope.aliyuncs.com/api/v1
RECA_WAN30_MODEL=wan3.0-video
RECA_WAN30_SUBMIT_PATH=/services/aigc/video-generation/video-synthesis
RECA_WAN30_STATUS_PATH=/tasks/{task_id}
RECA_WAN30_UPLOAD_PATH=/uploads
RECA_WAN30_STAGE_LOCAL_MEDIA=1
RECA_WAN30_RESOLUTION=720P
RECA_WAN30_RATIO=16:9
RECA_WAN30_PROMPT_EXTEND=false
RECA_WAN30_WATERMARK=false
RECA_WAN30_POLL_INTERVAL_S=5
RECA_WAN30_STATUS_LOG_INTERVAL_S=30
RECA_WAN30_TASK_TIMEOUT_S=3600

# Local gateway
RECA_GATEWAY_HOST=127.0.0.1
RECA_GATEWAY_PORT=8787
RECA_PUBLIC_BASE_URL=http://127.0.0.1:8787
# Optional Host-to-Gateway bearer token. Set the same value in the Gateway and
# Harness Host processes whenever 8787 is reachable beyond one trusted host.
RECA_GATEWAY_TOKEN=
# Optional persistent run store override (leave commented to use .dsh_runs).
# RECA_RUNS_ROOT=/persistent/path/.dsh_runs
# Optional stable opaque deployment identity used to prevent a DSH session from
# replaying a run id against a different Gateway. If omitted, the Gateway
# atomically persists a private `.gateway-instance-id` in RECA_RUNS_ROOT.
RECA_GATEWAY_INSTANCE_ID=
# Browser CORS is disabled by default because the DSH browser uses Host RPC.
# If a separate trusted browser client is unavoidable, list exact origins.
RECA_GATEWAY_ALLOW_ORIGIN=
RECA_LIST_RUNS_LIMIT=100
RECA_CANCEL_GRACE_S=15
RECA_DEMO_BACKEND=wan
RECA_DEMO_RESOLUTION=1280x720
# Wan remains the video backend; GPT Image2 renders portraits, locations,
# anchors, and image-edit repairs.
RECA_IMAGE_BACKEND=gpt-image-2
# Routify image endpoints are more reliable with ordinary JSON responses.
RECA_GPT_IMAGE_2_STREAM=0
# Set to `copy_source` only for gateways that support image generation but
# hang or reject /images/edits. It carries each anchor forward exactly.
RECA_GPT_IMAGE_2_EDIT_MODE=api
# Keep visual audit requests compact and bounded on OpenAI-compatible gateways.
RECA_AUDIT_MINIMAL_CONTEXT=1
RECA_GPT_AUDIT_TIMEOUT_S=60
RECA_GPT_AUDIT_MAX_TOKENS=1024
RECA_AUDIT_PROPAGATE_COPIED_ANCHORS=0
